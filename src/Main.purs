module Main where

import Prelude

-- import Euler (answer)
import ISicID (answer)
import Effect.Console (log)

-- main = do
--   log ("The answer is " <> show answer)

main = do
  log ("The answer is " <> answer)